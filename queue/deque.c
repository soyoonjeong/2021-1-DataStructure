#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#pragma warning(disable:4996)

typedef struct node {
	int elem;
	struct node* next;
}DEQUE;
int main() {
	DEQUE* front = NULL, * rear = NULL;
	int n;
	scanf("%d", &n);
}